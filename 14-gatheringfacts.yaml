- name: gathering facts from ansible # gathering complete server information from ansible 
  hosts: local '
  connection: local 
  tasks:
   - name: print the facts 
     ansible.builtin.debug:
      msg: "{{ ansible_facts }}""