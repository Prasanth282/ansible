- name: installing nginx in the server using yaml 
  hosts: node1
  become: yes 
  tasks: 
    - name: installing nginx 
      ansible.builtin.package:
        name: nginx
        state: present
  
    - name: installing nginx 
      ansible.builtin.package:
        name: nginx
        state: started
        enabled: yes